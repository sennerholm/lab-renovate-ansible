  steps:
  - script: |
      git clone https://github.com/actions/virtual-environments.git
      cd virtual-environments
      # Add renovate notification for changing
      # renovategr depname=actions/virtual-environments
      ref=releases/win22/20211219
      git checkout ${ref}

      // Need to matchS
      // renovatem depname=com.google.code.gson:gson
      // GSON_VERSION = "2.8.2"